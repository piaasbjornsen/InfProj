package eliteserien.core;

/** 
 *  Interface for Table change listeners. Notifies the listeners when the Table has changed.
 */ 

public interface TableListener {
    
    public void tableChanged(Table table);
}
